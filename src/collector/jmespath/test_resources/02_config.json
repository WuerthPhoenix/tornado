{
  "event_type": "${hostgroups[0]}",
  "payload": {
    "constant": "constant_value",
    "expression": "${icinga_customfields.snmpcommunity}",
    "with_null": null,
    "another_null": "${some_null_value}"
  }
}