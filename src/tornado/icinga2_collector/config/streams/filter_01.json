{
  "stream": {
    "types": ["StateChange"],
    "queue": "icinga2_StateChange_queue"
  },
  "collector_config": {
    "event_type": "${commits[0].committer.name}",
    "payload": {
      "source": "github",
      "ref": "${ref}",
      "repository_name": "${repository.name}"
    }
  }
}